#include "ft_printf.h"

int p_octal(t_components *m)
{
	if (CHECK_O(m->flags) && m->i++)
	{
		if (CHECK_OO(m->flags) && m->i++)
		{
			return (NULL); //how do I write this;
		}
	}
	else
	{
		
		
				
}



